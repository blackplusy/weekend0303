a.栈的方式访问列表
b.获取列表的索引
c.列表排序
d.列表推导式


a.栈的方式访问列表
	1.队列	先进先出
	2.栈    后进先出

b.获取列表的索引
	注意：只能是同类型数据进行操作，字符串和字符串，数字和数字
	注意：元素第一次出现位置的索引
	
c.列表的排序
	列表.sort()
	列表.reverse()

d.列表推导式
列表推导是又叫列表解析式，简单的方法创建列表
结构中包含一个表达式，一个语句中可以右0个或者多个if条件，返回结果是个列表
优势：
代码短,速度快
标准格式:
[表达式 for 变量 in 列表]
[表达式 for 变量 in 列表 if 条件]
